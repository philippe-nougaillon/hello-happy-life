<% if @groupe.sorties.any? %>
    <ul>
        <li>
            <b><%= l(@groupe.sorties.last.le, format: :long) %></b>
            <br> 
            <%= @groupe.sorties.last.lieu %>
        </li> 
    </ul>
<% end %>
